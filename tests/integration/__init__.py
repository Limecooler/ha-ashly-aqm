"""Live-device integration tests.

Excluded from the default pytest run. Opt in with `pytest -m integration`
and supply credentials via environment variables — see `tests/integration/
conftest.py` for the variables consumed.
"""
